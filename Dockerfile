# This file is a placeholder to easily run the application using docker-compose.
# For local development, please use: docker-compose up --build

FROM alpine:latest
CMD ["echo", "Please use 'docker-compose up --build' to run the application."]
